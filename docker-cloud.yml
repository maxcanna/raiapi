raiapi:
  autoredeploy: true
  environment:
    - NODE_ENV=production
    - PORT=80
    - 'REDISCLOUD_URL=redis://redis'
  image: 'maxcanna/raiapi:latest'
  links:
    - redis
  ports:
    - '80:80'
  restart: always
redis:
  expose:
    - '6379'
  image: 'redis:alpine'
  restart: always
